import React from 'react';

const Topbar = ()=>{
    return (
        <header>
            <input className="serch" type="text" placeholder="Search Movie"/>
        </header>
    )
};

export default Topbar;
